{
  "name": "jetchance-monorepo",
  "version": "1.0.1",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "start": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.3",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.13"
  }
}